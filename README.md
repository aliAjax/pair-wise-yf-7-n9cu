# 活字排版工坊

一个纯前端本地应用，用来维护字模库、在网格版面中落字、检查字模数量是否超用、保存草稿并导出PNG预览图。

支持词组连排：在字模库下方保存 2–6 字的词组，点击版面格子选定起始格后一键应用，按横排向右、竖排向下连续落字。每字优先取同字同风格（当前选中字模的风格）的字模，不足时回退同字其它未用尽的风格。应用前整条预检，越界、缺字模、字模用尽任一命中都会整体拒绝，状态条提示首个原因及对应字符，不留半截落字。词组随草稿一起保存、载入。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
